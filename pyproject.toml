[project]
name = "qr-pi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "John Silvell", email = "josil@MacBook-Pro.local" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "qrcode[pil]>=8.2",
]

[project.scripts]
qr-pi = "qr_pi:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
